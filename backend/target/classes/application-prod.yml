spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: Qaz123456789.
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 10

#mybatis-plus:
#  configuration:
#    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl

logging:
  level:
    com.obe: debug
  file:
    name: /app/logs/backend.log
